spring:
  application:
    name: fraud-service
  datasource:
    url: jdbc:mysql://localhost:3306/gaadix_fraud_db
    username: KD2-GANESH-92393
    password: manager
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8088

# Fraud Detection Configuration
fraud:
  detection:
    risk-threshold: 70
    high-risk-score: 80
