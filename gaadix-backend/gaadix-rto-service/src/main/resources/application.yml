spring:
  application:
    name: rto-service
  datasource:
    url: jdbc:mysql://localhost:3306/gaadix_rto_db
    username: KD2-GANESH-92393
    password: manager
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8085

# RTO Configuration
rto:
  mock:
    enabled: true
  verification:
    max-attempts: 3
